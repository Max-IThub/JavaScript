let users = []

function addUser(name, lastname, age) {
    users.map(name, lastname, age => ({name: name, lastname: lastname, age: age}))
    return user
}

addUser("Максим Николаев 18")

console.log(users)